[package]
name = "flatbuffers"
version = "1.0.2"
description = "Pure-Mojo FlatBuffers binary format encoder/decoder"
license = "MIT"
platforms = ["linux-64", "osx-arm64"]

[mojo]
requires = ">=0.26.2"

[dependencies]
